 'use client'

/**
 * Stub minimale dell'hook useActivePathSync.
 * L'implementazione reale può sincronizzare la rotta Next.js con lo stato globale (es. Zustand).
 */
export function useActivePathSync() {
  // no-op per ora
}

